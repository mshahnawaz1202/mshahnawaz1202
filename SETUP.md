# GitHub Profile Setup

1. Copy the contents of this folder into the `mshahnawaz1202` profile repository.
2. Keep the repository public and named exactly `mshahnawaz1202`.
3. Commit and push `README.md`, `assets/`, and `projects.json`.
4. The project panel works immediately because `assets/projects.svg` is committed directly to `main`.
5. The hero banner uses the supplied profile photo and supports GitHub dark/light themes.
